# Development and CI dependencies for FeltCrypto.
# Install: pip install -r requirements-dev.txt && pip install -e .
# Or use the pyproject extra: pip install -e ".[dev]"

-r requirements.txt

hypothesis>=6
mypy>=1.13
pytest>=8.3
pytest-cov>=5
ruff>=0.8
